<h1>Edit a Figure</h1>

<form action="/figures/<%=@figure.id%>" method="POST">

  Name: <input type="text" name="figure[name]" id="figure_name" value="<%=@figure.name%>"></input>
  <br></br>

  What NYC landmarks are associated with this figure? Choose from the list or add a new one!
  <br></br>
  <%Landmark.all.each do |landmark|%>
  <input type="checkbox" name="figure[landmark_ids][]" id="landmark_<%=landmark.id%>" value="<%=landmark.id%>" <%='checked' if @figure.landmarks.include?(landmark)%>><%=landmark.name%></input>
  <%end%>
  <br></br>

  New Landmark Name: <input type="text" name="landmark[name]" id="new_landmark"></input>
  <br></br>
  <br></br>

  What title does this figure hold? Choose from the list or add a new one!
  <br></br>
  <%Title.all.each do |title|%>
  <input type="checkbox" name="figure[title_ids][]" id="title_<%=title.id%>" value="<%=title.id%>" <%='checked' if @figure.titles.include?(title)%>><%=title.name%></input>
  <%end%>
  <br></br>

  New Title: <input type="text" name="title[name]" id="new_title"></input>

  <input type="submit" value="Edit Figure">
</form>
